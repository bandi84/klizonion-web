# Builder Agent Service

Reserved for the future agent orchestration/runtime layer.

The agent should eventually implement a structured loop such as:

PLAN → INSPECT → READ → EDIT → RUN → TEST → OBSERVE → FIX → EVALUATE → REPEAT

Do not add unrestricted machine control here.
